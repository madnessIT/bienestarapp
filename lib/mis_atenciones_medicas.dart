import 'package:flutter/material.dart';

class MisAtencionesMedicasPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Mis Atenciones Medicas'),
      ),
      body: Center(
        child: Text('Página de Mis atenciones medicas'),
      ),
    );
  }
}
